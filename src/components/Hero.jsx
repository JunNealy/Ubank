import React from 'react';
import styles from '../styles';
import { discount, robot } from '../assets';

const Hero = () => {
  return (
    <section
      id="home"
      className={`felx md:flex-row flex col ${styles.paddingY}`}
    ></section>
  );
};

export default Hero;
